#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        ll n, k;
        cin >> n >> k;
        vector<ll> arr(k * k), v(k);

        cout << k + ((k - 1) / (n - 1)) << endl;
    }
    return 0;
}